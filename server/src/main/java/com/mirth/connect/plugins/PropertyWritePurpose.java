/* Copyright (c) Mirth Corporation. Published under the MPL. */
package com.mirth.connect.plugins;

/** Trusted purpose selected by the engine route, never by a request body. */
public enum PropertyWritePurpose {
    NORMAL,
    RECOVERY
}
